
% data is N by M matrix where N = length of the input vector
% M = number of examples
% expected is nOutput by M matrix where P[i] = expected result from data[i]

function [data, expected] = readData(fName)
fprintf('reading data..');
%readin an actual text file 
data = randi(2, Model.nInput, Model.nExamples)-1;
expected = randn(Model.nOutput, Model.nExamples);
